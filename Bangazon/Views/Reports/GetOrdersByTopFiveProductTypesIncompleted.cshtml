@model IEnumerable<Bangazon.Models.AbandonedProductTypes>

@{
    ViewData["Title"] = "GetOrdersByTopFiveProductTypesIncompleted";
}

<h1>Top 5 Orders</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.OrderProducts.First().Product.ProductType.Label)
            </th>*@
            <th>Product Type</th>
            <th>Incomplete Orders</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @(item.ProductType.Label)
            </td>
            <td>
                @(item.Count)
            </td>

            @*<td>
            <a asp-action="Edit" asp-route-id="@item.ProductId">Edit</a>
            <a asp-action="Details" asp-route-id="@item.ProductId">Details</a>
            <a asp-action="Delete" asp-route-id="@item.ProductId">Delete</a>
        </td>*@
        </tr>
        }
    </tbody>
</table>